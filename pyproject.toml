[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "integration: real-model integration tests (network + model download + slow CPU inference) - excluded by default, run explicitly with `pytest -m integration`",
]
addopts = "-m \"not integration\""
